OPENAI_API_KEY=sk-REEMPLAZAR
OPENAI_MODEL=gpt-4o-mini

DATA_DIR=app/data
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

TOP_K=6
MAX_CHARS_PER_CHUNK=1200
CHUNK_OVERLAP=120

# Orígenes permitidos para CORS (opcional). En el mismo dominio no es necesario.
CORS_ORIGINS=*
MOCK_MODE=1
